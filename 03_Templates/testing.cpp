// Question 5:

// Write a function in C++ called turn_off_rightmost_set_bit that takes an integer n and returns the integer value after turning 
// off the rightmost set bit of n. Use a bitwise operator to accomplish this task.

// For example:

// If n = 12 (binary 1100), the result should be 8 (binary 1000).
// If n = 10 (binary 1010), the result should be 8 (binary 1000).
// How would you write the function?

int turn_off_rightmost_set_bit(int n){
    int a =1;
    int mask =1;
    return n;
}